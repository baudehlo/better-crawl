/** Kept in sync with package.json by the release process. */
export const VERSION = '0.2.0';

export const DEFAULT_USER_AGENT = `better-crawl/${VERSION} (+https://github.com/baudehlo/better-crawl)`;
